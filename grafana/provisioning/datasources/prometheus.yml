apiVersion: 1

datasources:
  # --- Prometheus : métriques d'infrastructure (cAdvisor) ---
  - name: Prometheus
    type: prometheus
    url: http://prometheus:9090
    isDefault: false
    editable: true
    jsonData:
      timeInterval: 15s
      httpMethod: POST
